package uni.test;

/**
 * Esta clase sirve para testear el constructor del Robot
 * Esta en otro paquete porque no tiene relacion directa con el robot.
 */
public class ConstructorRobotTest {

    /*
     * Estos son TESTS de las validaciones. Todos ellos deben fallar, por eso estan
     * comentados.
     * @param arg Argumentos desde la linea de comandos (no necesarios).
     */
    public static void main(String[] arg) {
        /*
                Mundo tierra = new Mundo("Mundo de Chocolate :)...", 10, 10);

                int cMax = 10; //Cantidad maxima
                int cCon = 10; //Cantidad conos

                //Testeo el mundo null
                Robot endrigo = new Robot(null, 5, 5, Robot.SUR, cMax, cCon);

                //Testeo fila negativa
         Robot endrigo1 = new Robot(tierra, -5, 5, Robot.SUR, cMax, cCon);

                //Testeo columna negativa
         Robot endrigo2 = new Robot(tierra, 5, -5, Robot.SUR, cMax, cCon);

                //Testeo limite inferior direccion
                Robot endrigo3 = new Robot(tierra, 5, 5, -1, cMax, cCon);

                //Testeo limite superior direccion
                Robot endrigo4 = new Robot(tierra, 5, 5, 4, cMax, cCon);

                //Testeo que cantConos < cantMax
                Robot endrigo5 = new Robot(tierra, 5, 5, Robot.SUR, 50, 100);

                //Testeo que cantConos = cantMax
                Robot endrigo6 = new Robot(tierra, 5, 5, Robot.SUR, 50, 50);

                //Testeo que cantMax no sea negativa
         Robot endrigo7 = new Robot(tierra, 5, 5, Robot.SUR, -cMax, cCon);

                //Testeo que cantConos no sea negativa
         Robot endrigo8 = new Robot(tierra, 5, 5, Robot.SUR, cMax, -cCon);

                //Testeo fila robot sobre limite mundo
         Robot endrigo9 = new Robot(tierra, 10, 5, Robot.SUR, cMax, cCon);

                //Testeo columna robot sobre limite mundo
         Robot endrigo10 = new Robot(tierra, 5, 10, Robot.SUR, cMax, cCon);
         */
    }

}
