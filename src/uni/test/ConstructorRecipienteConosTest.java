package uni.test;

/**
 * Esta clase sirve para testear el constructor de la clase RecipienteCono
 * Esta en otro paquete porque no tiene relacion directa con el robot.
 */
public class ConstructorRecipienteConosTest {

    /**
     * Estos son TESTS de las validaciones. Todos ellos deben fallar, por eso estan
     * comentados.
     * @param arg Argumentos desde la linea de comandos (no necesarios).
     */
    public static void main(String[] arg) {
        /*
                int fila = 10;
                int columna = 10;
                int cMax = 10; //Cantidad maxima
                int cCon = 10; //Cantidad conos

         Mundo tierra = new Mundo("Vieron este mundo? ...", fila, columna);

                //Testeo mundo null
         RecipienteConos c = new RecipienteConos(null, fila, columna, cMax, cCon);

                //Testeo columna negativa
         RecipienteConos c1 = new RecipienteConos(tierra, 5, -columna, cMax,
                                                         cCon);

                //Testeo fila negativa
         RecipienteConos c2 = new RecipienteConos(tierra, -fila, 5, cMax,
                                                         cCon);

                //Testeo columna > cantColumnasMundo
         RecipienteConos c3 = new RecipienteConos(tierra, fila, 11, cMax, cCon);

                //Testeo fila > cantFilasMundo
                RecipienteConos c4 = new RecipienteConos(tierra, 11, 5, cMax,
                                                         cCon);

                //Testeo columna cero
         RecipienteConos c5 = new RecipienteConos(tierra, 5, 0, cMax, cCon);

                //Testeo fila cero
         RecipienteConos c6 = new RecipienteConos(tierra, 0, 5, cMax, cCon);

                //Testeo columna sobre limite superior mundo
         RecipienteConos c7 = new RecipienteConos(tierra, 5, columna, cMax, cCon);

                //Testeo fila sobre limite superior del mundo
         RecipienteConos c8 = new RecipienteConos(tierra, fila, 5, cMax, cCon);

                //Testeo que cantConos < cantMax
         RecipienteConos c9 = new RecipienteConos(tierra, 5, 5, cMax, 11);

                //Testeo que cantConos = cantMax
         RecipienteConos c10 = new RecipienteConos(tierra, 5, 5, cMax, 10);

                //Testeo que cantMax no sea negativa
         RecipienteConos c11 = new RecipienteConos(tierra, 5, 5, -cMax, -100);

                //Testeo que cantConos no sea negativa
         RecipienteConos c12 = new RecipienteConos(tierra, 5, 5, cMax, -cCon);
         */
    }

}
